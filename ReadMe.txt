Dentro do relatório temos as seguintes classificações suportadas: blocker, critical, normal, minor, trivial. 
Também é possível colocar dentro dos cenários a que plano de teste e que tratativa ele se refere
A serem postas antes dos cenários:

@severity=blocker  
Cenário: XPTO

 
Para instalar o report
```install
npm install -g allure-commandline
```

Para executar o report
```install
allure serve allure-report
```

